# Contributing to QuickSight Refresh Workflow

Thank you for your interest in contributing to the QuickSight Refresh Workflow project!

## How to Contribute

1. Fork the repository
2. Create a new branch (`git checkout -b feature/amazing-feature`)
3. Make your changes
4. Run tests
5. Commit your changes (`git commit -m 'Add amazing feature'`)
6. Push to the branch (`git push origin feature/amazing-feature`)
7. Open a Pull Request

## Development Setup

1. Install dependencies:
```bash
pip install -r src/lambda/initiator/requirements.txt
pip install -r src/lambda/status_checker/requirements.txt
